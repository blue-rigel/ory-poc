# Ory Network project API key (Console > Project Settings > API Keys).
# Server-only. Never prefix with NEXT_PUBLIC_.
ORY_PROJECT_API_TOKEN=

# Ory OAuth client credentials for this portal only.
ORY_CLIENT_ID=
ORY_CLIENT_SECRET=

# Generate locally with: openssl rand -base64 32
AUTH_SECRET=
